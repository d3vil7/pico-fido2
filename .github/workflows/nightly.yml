name: "Nightly deploy"

on:
  workflow_dispatch:

jobs:
  nightly:
    name: Deploy nightly
    strategy:
      fail-fast: false
      matrix:
        refs: [main]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: ${{ matrix.refs }}
          submodules: 'recursive'
      - name: Patch pico-keys-sdk version
        run: |
          sed -i 's/\(SET_VERSION(ver_major ver_minor.*pico_keys_version\.h"\) 1)/\2 2)/' pico-keys-sdk/CMakeLists.txt
      - name: Restore private key
        run: |
          echo "${{ secrets.PRIVATE_KEY_B64 }}" | base64 -d > private.pem
          chmod 600 private.pem
      - name : Build
        env:
          PICO_SDK_PATH: ../pico-sdk
          SECURE_BOOT_PKEY: ../private.pem
        run: |
           ./workflows/autobuild.sh pico
           ./build_pico_fido2.sh
      - name: Delete private key
        run: rm private.pem
      - name: Update nightly release
        uses: pyTooling/Actions/releaser@main
        with:
          tag: nightly-${{ matrix.refs }}
          rm: true
          token: ${{ secrets.GITHUB_TOKEN }}
          files: release/*.*
